import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,
  document.getElementById('root')
);

// npm install --save react-router-dom   ->> para baixar biblioteca de rotas para a troca de paginas.